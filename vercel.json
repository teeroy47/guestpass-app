{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- app/ lib/ components/ public/ || exit 0"
}